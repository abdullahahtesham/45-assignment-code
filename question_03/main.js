//Name Cases
var myName = "Abdullah";
console.log(myName.toLowerCase()); //this will print the name in the small letter
console.log(myName.toUpperCase()); //this will print the name in big letter
console.log(myName.charAt(0).toUpperCase() + myName.slice(1).toLowerCase()); //this is the title case
